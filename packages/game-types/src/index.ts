/**
 * Package identity marker.
 *
 * Placeholder entrypoint validating workspace build/type-resolution wiring.
 * Real domain types are introduced by future scoped tasks.
 */
export const PACKAGE_NAME = "@pokenexus/game-types" as const;
