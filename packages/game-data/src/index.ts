/**
 * Package identity marker.
 *
 * Placeholder entrypoint validating workspace build/type-resolution wiring.
 * Real static game data definitions are introduced by future scoped tasks.
 */
export const PACKAGE_NAME = "@pokenexus/game-data" as const;
